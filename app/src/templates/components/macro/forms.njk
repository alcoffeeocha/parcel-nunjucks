{% macro field(name,value='',type='text',placeholder='') %}
<div class="field">
  <input type="{{ type }}" name="{{ name }}"
         value="{{ value | escape }}" placeholder="{{placeholder}}" />
</div>
{% endmacro %}